<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/pm_bkg"
    android:id="@+id/flFragmentContainer"
    tools:context="com.peermountain.scan_id_sdk.scan.ScanIdentityDocumentCameraActivity">

   <!-- <TextView
        android:id="@+id/tvMsg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_margin="@dimen/pm_margin_xbig"
        android:text="@string/pm_msg_capture_mrz"
        android:textSize="24sp"
        android:textColor="@color/pm_share_text"
        android:visibility="gone"
        tools:visibility="visible"/>

    &lt;!&ndash; Camera &ndash;&gt;
    <com.otaliastudios.cameraview.CameraView
        android:id="@+id/camera"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="@dimen/pm_camera_size"
        android:layout_gravity="center"
        android:keepScreenOn="true"
        app:cameraAudio="off"
        app:cameraCropOutput="true"
        app:cameraFacing="back"
        app:cameraFlash="off"
        app:cameraGestureLongTap="focusWithMarker"
        app:cameraGesturePinch="zoom"
        app:cameraGestureScrollHorizontal="exposureCorrection"
        app:cameraGestureScrollVertical="none"
        app:cameraGestureTap="focusWithMarker"
        app:cameraGrid="off"
        app:cameraJpegQuality="100"
        app:cameraSessionType="picture"
        app:cameraPlaySounds="true"
        app:cameraPictureSizeMinWidth="2000"
        app:cameraPictureSizeMaxWidth="2500"
        app:cameraPictureSizeBiggest="false"
        />

    <ImageView
        android:id="@+id/btnRecord"
        style="@style/camera_button"
        android:background="@drawable/pm_circle_btn"
        android:padding="12dp"
        android:visibility="gone"
        app:srcCompat="@drawable/pm_ic_photo"
        tools:visibility="visible"/>

    <Button
        android:id="@+id/btnDone"
        android:drawableLeft="@drawable/pm_ic_check_24dp"
        android:drawablePadding="@dimen/pm_margin_small"
        android:text="@string/pm_btn_done_scanning"
        android:visibility="gone"
        tools:visibility="visible"
        style="@style/camera_extra_buttons"/>

    <Button
        android:id="@+id/btnFlash"
        style="@style/camera_extra_buttons"
        android:layout_gravity="bottom|left"
        android:text="@string/pm_btn_flash"
        android:visibility="gone"
        tools:visibility="visible"/>

    <ProgressBar
        android:id="@+id/progress"
        style="@style/camera_button"
        android:visibility="visible"/>-->
</FrameLayout>
